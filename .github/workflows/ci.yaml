name: ci

on:
  push: 

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: titi
        uses: actions/checkout@v4
      
      - name: Login Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build et Push
        run: |
          docker build -t davidbekale/mon-image .
          docker push davidbekale/mon-image
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: tata
        uses: actions/checkout@v4
      
      - name: Login Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build et Push
        run: |
          docker build -t davidbekale/mon-app .
          docker push davidbekale/mon-app